branches:
    only:
        - master
skip_tags: true
matrix:
    fast_finish: true
build_script:
    - git submodule init
    - git submodule update
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
    - ctest